# VoiceNote Backend API - Environment Variables
# Copy this file to .env and update with your values
# 
# IMPORTANT: This is a TEXT file because .env files are gitignored
# Rename this to .env after copying: mv env.example.txt .env

# Server Configuration
PORT=3333
CORS_ORIGIN=https://your-frontend-domain.com

# Database
DB_PATH=./voicenotes.db

# Upload Directory
UPLOAD_DIR=./uploads

# JWT Secret (change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# Note: API Keys (Gemini, Groq) are NOT needed here!
# 
# The backend only handles:
# - Database sync (SQLite)
# - File storage (audio uploads)
# - API endpoints
#
# All AI features (transcription, image processing, chat) run CLIENT-SIDE
# in the frontend. Configure those API keys in the FRONTEND .env file.
#
# See: ../../ENV_SETUP.md for complete environment variable documentation
# ============================================

